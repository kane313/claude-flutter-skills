// Generated by flutter-html-reproduce
// Source: {{source_meta_path}}
// Phase 1 (skeleton) · {{timestamp}}
// {{#if vision_only}}Vision-only inference — confidence: low{{/if}}

import 'package:flutter/material.dart';

class {{page_class}} extends {{stateful_or_stateless}} {
  const {{page_class}}({super.key});

{{#if stateful}}
  @override
  State<{{page_class}}> createState() => _{{page_class}}State();
}

class _{{page_class}}State extends State<{{page_class}}> {
{{#each form_controllers}}
  final TextEditingController _{{name}} = TextEditingController();
{{/each}}
{{#if has_form}}
  final GlobalKey<FormState> _formKey = GlobalKey<FormState>();
{{/if}}

  @override
  void dispose() {
{{#each form_controllers}}
    _{{name}}.dispose();
{{/each}}
    super.dispose();
  }

{{/if}}
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: {{root_widget_tree}},
    );
  }
}

{{#each private_widgets}}

class _{{class_name}} extends StatelessWidget {
  const _{{class_name}}({super.key});

  @override
  Widget build(BuildContext context) {
    return {{widget_tree}};
  }
}
{{/each}}
